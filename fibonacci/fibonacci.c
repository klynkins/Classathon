#include <cs50.h>
#include <stdio.h>

int sumFibs(void);

int main(void)
{
    int n = get_int("Sum of Fibonacci numbers is : ");
    printf = ("%i", n)
}

int sumFibs(void)
{
    int a = 0;
    int b = 1;
    int count = 0;

    while(b <= num)
    {
    [b, a] = [b+a, b]
    if(a%2 !==0)
    {
      count+=a
    }
    }
    return count;
}

